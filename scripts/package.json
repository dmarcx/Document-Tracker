{
  "name": "document-tracker-reminders",
  "version": "1.0.0",
  "description": "Daily document expiry reminder emails via Firebase Admin + Nodemailer",
  "main": "sendReminders.js",
  "scripts": {
    "start": "node sendReminders.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "nodemailer": "^6.9.0"
  },
  "engines": {
    "node": ">=18"
  }
}
